import React from "react";

class Init extends React.Component {
  render() {
    return (
      <div>
        <button>Mesero</button>
        <button>Cocinero</button>
      </div>
    );
  }
}

export default Init;
